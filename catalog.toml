format = 1
name = "forkstify"
description = "forkstify's reference catalog — one TOML card per artist."

# Default proximity of each link type (1 = a distant echo, 5 = almost the
# same universe). This is a setting of YOUR algorithm: change it, commit it.
[proximity]
member = 5
collab = 4
similar = 4
family = 3
scene = 3
influence = 2
